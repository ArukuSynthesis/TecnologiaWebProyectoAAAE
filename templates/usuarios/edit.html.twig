{% extends 'base.html.twig' %}

{% block title %}Edit Usuario{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1>Edit Usuario</h1>

    {{ include('usuarios/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_usuarios_index') }}" class="btn btn-secondary mt-3">Back to list</a>

    {{ include('usuarios/_delete_form.html.twig') }}
</div>
{% endblock %}
